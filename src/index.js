/* The index.js file is the most important file */
/* This is where we connect our React app to the DOM */
import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

